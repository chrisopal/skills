# Python dependencies for the bundled scripts.
# Node dependency `pptxgenjs` is prepared by scripts/ppt_renderer.py via npm.
httpx>=0.28
PyYAML>=6.0
python-pptx>=1.0.2
Pillow>=11.0
pytest>=7
jsonschema>=4
