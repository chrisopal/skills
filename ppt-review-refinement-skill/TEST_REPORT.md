# Test Report

测试日期：2026-08-07

## 已验证

- 所有 JSON 模板和示例通过 Draft 2020-12 JSON Schema 校验。
- Python 脚本通过 `compileall` 语法检查。
- 自动生成 4 页不一致测试 PPTX。
- 成功生成结构审计、逐页 PNG 和总览图。
- 成功执行字体统一、精确颜色映射、标题字号和标题位置规范化，共记录 42 项变更。
- 成功验证页数、正文、标题授权、数字与单位、受保护术语、图片哈希、结构风险和页面渲染。
- 候选文件无 P0/P1 结构风险；4 页全部成功渲染。
- 质量门禁回归测试 6 项通过：批准身份、非法 manifest、未接受链接风险、已接受链接风险、未改图片和明显文本溢出。
- 未登记的人工复核风险会使验证失败；已登记的风险只返回 `pass_with_accepted_risks`。
- L2/L3 执行器回归测试通过：需要批准的 manifest 和 pilot，执行几何/字体角色白名单动作，输出可重新打开并记录变更。
- 叙事/视觉报告编排回归测试通过：合并结果通过 `review_report.schema.json`。
- 最终人工视觉签字回归测试通过：缺失 `visual_signoff.json` 时最终验证失败，完整批准签字可通过门禁。

## 说明

自动校验之后仍要求人工逐页查看渲染图并提交 `visual_signoff.json`；这是 Skill 的强制质量门禁，而不是测试失败。复杂对象仍需外部执行器或 PowerPoint/Office API 复核。
